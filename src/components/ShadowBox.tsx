import Box from '@mui/material/Box';
import { styled } from '@mui/material/styles';

export const ShadowBox = styled(Box)(({ }) => ({
    boxShadow: "0px 7px 12px 0px rgba(100, 100, 111, 20%)",
    backgroundColor: '#FFFFFF',
    flex: 1,
}))
